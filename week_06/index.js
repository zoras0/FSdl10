import React from "react";
import ReactDOM from "react-dom";
import "./index.css"; // Import default CSS file
import App from "./App"; // Import the App component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
